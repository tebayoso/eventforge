apiVersion: v1
kind: Service
metadata:
  name: {{ include "eventforge.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector: { app.kubernetes.io/name: {{ include "eventforge.name" . }} }
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
