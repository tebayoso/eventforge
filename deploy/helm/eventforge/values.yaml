image:
  repository: ghcr.io/tebayoso/eventforge-control-plane
  tag: "0.1.0"
  # Release automation must override this with the published multi-arch digest.
  digest: ""
  pullPolicy: IfNotPresent
# The current store is process-local. Keep a single replica until workflow, run, and memory state are shared.
replicaCount: 1
service:
  type: ClusterIP
  port: 4310
ingress:
  enabled: false
  className: ""
  host: eventforge.example.com
postgres:
  existingSecret: eventforge-postgres
  databaseUrlKey: database-url
secrets:
  existingSecret: eventforge-runtime
  openaiApiKeyKey: openai-api-key
  encryptionKeyKey: encryption-key
providerSecrets:
  github:
    webhookEnabled: false
    webhookSecretKey: github-webhook-secret
    appCredentialsEnabled: false
    appIdKey: github-app-id
    appPrivateKeyKey: github-app-private-key
  linear:
    webhookEnabled: false
    webhookSecretKey: linear-webhook-secret
    oauthEnabled: false
    clientIdKey: linear-client-id
    clientSecretKey: linear-client-secret
  sentry:
    webhookEnabled: false
    webhookSecretKey: sentry-webhook-secret
    authTokenEnabled: false
    authTokenKey: sentry-auth-token
env:
  EVENTFORGE_RUNTIME_MODE: remote
  EVENTFORGE_HOST: "0.0.0.0"
  EVENTFORGE_CODEX_WORKDIR: "/workspaces"
  EVENTFORGE_RUNNER: codex
  EVENTFORGE_DEMO_MODE: "false"
  EVENTFORGE_ALLOWED_ORIGINS: "https://eventforge.dev"
cloudflareTunnel:
  enabled: false
  replicaCount: 2
  image:
    repository: cloudflare/cloudflared
    tag: "2026.7.2"
    digest: "sha256:4f6655284ab3d252b7f28fedb19fe6c8fc82ee5b1295c20ac74d475e5398a52d"
    pullPolicy: IfNotPresent
  existingSecret: eventforge-cloudflared
  tokenKey: tunnel-token
  podSecurityContext:
    runAsNonRoot: true
    seccompProfile:
      type: RuntimeDefault
  containerSecurityContext:
    allowPrivilegeEscalation: false
    capabilities:
      drop: ["ALL"]
    readOnlyRootFilesystem: true
  resources:
    requests: { cpu: 50m, memory: 64Mi }
    limits: { cpu: 250m, memory: 128Mi }
resources:
  requests: { cpu: 250m, memory: 512Mi }
  limits: { cpu: "1", memory: 1Gi }
podSecurityContext:
  runAsNonRoot: true
  runAsUser: 1000
  runAsGroup: 1000
  fsGroup: 1000
containerSecurityContext:
  allowPrivilegeEscalation: false
  capabilities:
    drop: ["ALL"]
  readOnlyRootFilesystem: true
  seccompProfile:
    type: RuntimeDefault
