# beta.eventforge.dev build.
#
# Analytics are deliberately DISABLED here. Both providers are gated on a
# non-empty key (src/analytics.ts), so leaving these blank keeps beta traffic out
# of the production GA4 property and PostHog project instead of polluting them
# with test sessions.
VITE_GA_MEASUREMENT_ID=
VITE_POSTHOG_KEY=
