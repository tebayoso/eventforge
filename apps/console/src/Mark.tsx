// Shared by LandingPage and FeaturesPage so the brand lockup stays identical
// across both public routes.
export function Mark() {
  return <img className="ef-mark" src="/eventforge-mark.svg" alt="" aria-hidden="true" />;
}
